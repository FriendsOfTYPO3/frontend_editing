machine:
  php:
    version: 7.0.4

## Customize test commands
test:
  override:
    - npm run build:suite --silent

deployment:
  production:
    branch: master
    commands:
      - ./deploy_production.sh